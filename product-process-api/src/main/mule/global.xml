<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd">

    <configuration-properties file="properties/${env}.yaml"/>

    
    <http:listener-config name="HTTP_Listener_Config">
        <http:listener-connection host="${http.host}" port="${http.port}" />
    </http:listener-config>

    
    <http:request-config name="FakeStore_System_Config">
        <http:request-connection 
            protocol="${fakestore.protocol}"
            host="${fakestore.host}"
            port="${fakestore.port}" />
    </http:request-config>

   
    <vm:config name="VM_Config">
        <vm:queues>
            <vm:queue queueName="product.sync.queue"/>
        </vm:queues>
    </vm:config>
 
<http:request-config name="Salesforce_System_Config">
    <http:request-connection 
        protocol="${salesforce.protocol}"
        host="${salesforce.host}"
        port="${salesforce.port}" />
</http:request-config>
  
</mule>
